/*
 * character offsets table for writing
 * chars with offset and kerning
 *
 * the offset is the number of blank
 * pixels at trailing edge of character
 *
 */

#ifndef __OFFSET_H
#define __OFFSET_H

extern const uint8_t offset[];

#endif
